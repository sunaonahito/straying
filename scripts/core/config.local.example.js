/**
 * Research Novel Framework
 * Local Configuration Example
 *
 * このファイルをconfig.local.jsとしてコピーし、
 * 環境ごとの送信先URLを設定する。
 */

const RNF_LOCAL_CONFIG = {
  ENDPOINT_URL: "",
};

export default RNF_LOCAL_CONFIG;